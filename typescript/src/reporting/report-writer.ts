import fs from 'node:fs';import path from 'node:path';import type {Report} from '../model.js';export function writeJson(r:Report,p:string){fs.mkdirSync(path.dirname(path.resolve(p)),{recursive:true});fs.writeFileSync(p,JSON.stringify(r,null,2));}export function writeMarkdown(r:Report,p:string){const avg=r.files.length?r.files.reduce((n,x)=>n+(x.risk.score as number),0)/r.files.length:0;const lines=['# Quality Report — TypeScript','',`Overall Risk: ${avg.toFixed(1)} / 100`,'','## Summary','','| Metric | Value |','|---|---:|',`| Files | ${r.files.length} |`,`| Critical | ${r.files.filter(x=>x.risk.level==='critical').length} |`,`| Hotspots | ${r.files.filter(x=>+x.risk.score!>=60).length} |`];if(r.warnings.length)lines.push('','## Warnings','',...r.warnings.map(x=>`- ${x}`));lines.push('','## Top Hotspots','','| File | Risk | Changes | Rework | Complexity | Coverage |','|---|---:|---:|---:|---:|---:|',...r.files.filter(x=>+x.risk.score!>=60).sort((a,b)=>+b.risk.score!-+a.risk.score!).slice(0,20).map(x=>`| ${x.path} | ${x.risk.score} | ${x.history.commitCount} | ${(x.history.reworkRate*100).toFixed(0)}% | ${x.metrics.complexity} | ${x.metrics.lineCoverage===undefined?'Not Available':x.metrics.lineCoverage.toFixed(1)+'%'} |`),'','## Change Coupling','','| File | Coupled With | Ratio |','|---|---|---:|',...r.files.flatMap(x=>x.couplings).sort((a,b)=>b.ratio-a.ratio).slice(0,20).map(x=>`| ${x.file} | ${x.coupledWith} | ${(x.ratio*100).toFixed(0)}% |`),'','## High Complexity Functions','','| File | Function | Complexity |','|---|---|---:|',...r.files.flatMap(x=>x.functions.map(f=>({path:x.path,...f}))).sort((a,b)=>b.complexity-a.complexity).slice(0,20).map(x=>`| ${x.path} | ${x.name} | ${x.complexity} |`));fs.mkdirSync(path.dirname(path.resolve(p)),{recursive:true});fs.writeFileSync(p,lines.join('\n'));}
