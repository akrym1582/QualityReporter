import test from 'node:test';import assert from 'node:assert/strict';import {calculateUntestedComplexity} from '../src/analysis/untested-complexity.js';import {calculateDuplicationRisk} from '../src/analysis/duplication-risk.js';import {parseJscpdReport} from '../src/duplication/jscpd-report-parser.js';import {mapDuplicates} from '../src/duplication/duplicate-symbol-mapper.js';import type {FileResult} from '../src/model.js';
test('untested complexity handles coverage boundaries and missing coverage',()=>{assert.equal(calculateUntestedComplexity(34,42),19.72);assert.equal(calculateUntestedComplexity(34,100),0);assert.equal(calculateUntestedComplexity(3,0),3);assert.equal(calculateUntestedComplexity(34),undefined);assert.equal(calculateUntestedComplexity(10,40,80,'combined'),4.4)});
test('duplicate risk prioritizes activity',()=>assert.ok(calculateDuplicationRisk(.4,90)>calculateDuplicationRisk(.4,10)));
test('jscpd parser maps partial overlap',()=>{const groups=parseJscpdReport('{"duplicates":[{"lines":6,"tokens":60,"firstFile":{"name":"src/a.ts","start":5,"end":10},"secondFile":{"name":"src/b.ts","start":1,"end":6}}]}');const file:FileResult={path:'src/a.ts',metrics:{loc:20,complexity:1,issues:{error:0,warning:0,info:0}},history:{commitCount:0,churn:0,authorCount:0,reworkRate:0},activity:{},risk:{},hotspot:{priorityScore:0,classifications:[]},recommendations:[],couplings:[],functions:[],duplicateGroups:[],symbols:[{symbolId:'a'.repeat(64),symbolKey:'a',name:'m',kind:'method',location:{startLine:8,endLine:15},metrics:{loc:8,complexity:1},history:{commitCount:0,churn:0,authorCount:0,reworkRate:0},activity:{},risk:{},hotspot:{priorityScore:0,classifications:[]},couplings:[],recommendations:[],duplicateGroups:[]}]};mapDuplicates(groups,[file]);assert.equal(file.metrics.duplication?.duplicatedLines,6);assert.equal(file.symbols![0].metrics.duplication?.duplicatedLines,3)});
test('invalid jscpd report is rejected',()=>assert.throws(()=>parseJscpdReport('{'),/Invalid jscpd/));
